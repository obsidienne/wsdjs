<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Accept-CH" content="DPR, Viewport-Width, Width">
    <meta name="cloudinary_cloud_name" content="don2kwaju">
    <meta name="description" content="">
    <meta name="author" content="">

    <title><%= page_title(@conn) %></title>

    <link rel="stylesheet" type="text/css" href="<%= static_path(@conn, "/css/critical.css") %>">
    <link rel="preload" href="<%= static_path(@conn, "/css/defered.css") %>" as="style" onload="this.rel='stylesheet'">

    <script defer src="<%= static_path(@conn, "/js/app.js") %>"></script>
    <script defer src="//widget.cloudinary.com/global/all.js" type="text/javascript"></script>
    <%= if user = @conn.assigns[:current_user] do %>
    <%= tag :meta, name: "channel_token", content: Phoenix.Token.sign(@conn, "user", user.id) %>
    <% end %>

    <%= render_existing(@view_module, "_meta." <> @view_template, assigns) %>
  </head>

  <body class="<%= js_view_name(@conn, @view_template) %>" data-js-view-name="<%= js_view_name(@conn, @view_template) %>">
    <%= render @view_module, @view_template, assigns %>

    <%= render "_player.html", conn: @conn %>

    <div id="notifier-container" class="notifier-container">
      <%= if get_flash(@conn, :info) do %>
      <div id="info-notification" class="notifier info" role="alert">
        <h2 class="notifier-title"><%= get_flash(@conn, :info) %></h2>
      </div>
      <% end %>

      <%= if get_flash(@conn, :error) do %>
      <div id="error-notification" class="notifier danger" role="alert">
        <h2 class="notifier-title"><%= get_flash(@conn, :error) %></h2>
      </div>
      <% end %>
    </div>

    <%= if Mix.env == :prod, do: render "_piwik.html" %>
  </body>
</html>
